package com.chienpm.zimage.memory_layer;

import android.graphics.Bitmap;

import androidx.annotation.NonNull;

/***
 *  MemoryCache is the master class of Memory Caching layer of library
 *  This class will holding a cache-memory which store bitmaps
 *  Get bitmap from key which mapping to image url view @ImageMapper
 */
public class MemoryCache {

    public static Bitmap getBitmap(@NonNull String url) {
        return null;
    }

    public static void saveBitmap(String key, Bitmap bitmap) {

    }
}
